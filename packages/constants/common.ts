import { defineMap } from '@element-plus-leafer/utils';

export const presetFont = 'Inter, Helvetica Neue, Helvetica, PingFang SC, Hiragino Sans GB, Microsoft YaHei, 微软雅黑, Arial, sans-serif';

export const sizeMap = defineMap({
  'large': 40,
  'default': 32,
  'small': 24,
});

export const borderRadiusMap = defineMap({
  'base': 4,
  'small': 2,
  'round': 20,
  'circle': 100,
});
